@model IEnumerable<eMat.DA.tbCarrera>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.carrera)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbPlanEstudio.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.carrera)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbPlanEstudio.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.sigla }) |
            @Html.ActionLink("Details", "Details", new { id=item.sigla }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.sigla })
        </td>
    </tr>
}

</table>
